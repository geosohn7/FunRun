{
    "name": "fun-run-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "start:backend": "npm run start:dev --workspace=apps/backend-server",
        "start:mobile": "cd apps/mobile_app_flutter && flutter run",
        "build:backend": "npm run build --workspace=apps/backend-server"
    }
}